import React from "react";
import HomePage from "@/components/main";

const title = "Virtual BookStore";

const Home = () => {
  return( 
    <div>
        <title>{title}</title>
        <HomePage />
    </div>
  );
};

export default Home;
